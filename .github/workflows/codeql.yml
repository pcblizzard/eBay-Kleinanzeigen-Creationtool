name: CodeQL

# Statische Sicherheitsanalyse von GitHub. Die Ergebnisse erscheinen im Tab
# "Security" und sind damit fuer Nutzer des Projekts nachvollziehbar - anders
# als eine blosse Selbstauskunft in der Dokumentation.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    # Woechentlich, damit auch neu bekannt gewordene Muster greifen.
    - cron: '17 4 * * 1'
  workflow_dispatch:

jobs:
  analyze:
    name: Analyse
    runs-on: ubuntu-latest
    # Code-Scanning setzt auf privaten Repositories GitHub Advanced Security
    # voraus. Ohne das kann die Analyse ihre Ergebnisse nicht abliefern und
    # der Job faellt, obwohl am Quelltext nichts falsch ist. Statt dauerhaft
    # rot zu stehen - und damit echte Fehler unsichtbar zu machen - wird er
    # uebersprungen und laeuft von selbst wieder an, sobald das Repository
    # oeffentlich ist.
    if: github.event.repository.private == false
    permissions:
      security-events: write
      contents: read
      # Die Action liest Angaben zum eigenen Lauf; ohne diese Berechtigung
      # meldet sie "Resource not accessible by integration" und der Job faellt,
      # obwohl die Analyse selbst durchgelaufen ist.
      actions: read

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Initialise CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: python
          # security-and-quality meldet zusaetzlich Wartbarkeitsmuster.
          queries: security-and-quality

      - name: Run analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: /language:python
